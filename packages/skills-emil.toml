# Agent skills from github.com/emilkowalski/skills, following its main branch.
# oku.lock records the commit, so every machine links the same files.
# `oku update skills-emil` takes the newest commit.

[package]
name = "skills-emil"
description = "Agent skills by Emil Kowalski, animation and design"
homepage = "https://github.com/emilkowalski/skills"

[version]
from = "git-branch"
repo = "https://github.com/emilkowalski/skills"
branch = "main"

# The package only holds files. lists/ai.toml links the skills out of it.
[build]
needs = ["git"]
source = { git = "https://github.com/emilkowalski/skills", tag = "{{tag}}" }

[[build.step]]
run = """
set -eu
rm -rf .git
cp -R . {{prefix}}
"""
shell = "sh"
